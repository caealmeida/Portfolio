import { Flex, Text } from '@chakra-ui/react';

const About = () => (
	<Flex id="about" p={2} mt={600} border="1px" borderColor="red">
		<Text fontSize="2.5em">Sobre</Text>
	</Flex>
);

export default About;
